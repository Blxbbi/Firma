const canvas = document.getElementById('game'); const ctx = canvas.getContext('2d'); let score = 0; let snake = [{x: 100, y: 100}, {x: 90, y: 100}, {x: 80, y: 100}]; let food = {x: Math.floor(Math.random() * 30) * 10, y: Math.floor(Math.random() * 30) * 10}; let direction = 'right'; let gameInterval; document.addEventListener('keydown', (e) => { if (e.key === 'ArrowUp' && direction !== 'down') direction = 'up'; else if (e.key === 'ArrowDown' && direction !== 'up') direction = 'down'; else if (e.key === 'ArrowLeft' && direction !== 'right') direction = 'left'; else if (e.key === 'ArrowRight' && direction !== 'left') direction = 'right'; }); function draw() { ctx.clearRect(0, 0, canvas.width, canvas.height); ctx.fillStyle = '#0f0'; ctx.fillRect(food.x, food.y, 10, 10); ctx.fillStyle = '#fff'; for (let i = 0; i < snake.length; i++) { ctx.fillRect(snake[i].x, snake[i].y, 10, 10); } ctx.fillStyle = '#fff'; ctx.font = '16px Pixel'; ctx.textAlign = 'left'; ctx.textBaseline = 'top'; ctx.fillText('Score: ' + score, 10, 10); } function update() { let head = snake[0]; let newHead = {x: head.x, y: head.y}; if (direction === 'up') newHead.y -= 10; else if (direction === 'down') newHead.y += 10; else if (direction === 'left') newHead.x -= 10; else if (direction === 'right') newHead.x += 10; snake.unshift(newHead); if (snake[0].x === food.x && snake[0].y === food.y) { score++; food = {x: Math.floor(Math.random() * 30) * 10, y: Math.floor(Math.random() * 30) * 10}; } else snake.pop(); if (snake[0].x < 0 || snake[0].x > canvas.width - 10 || snake[0].y < 0 || snake[0].y > canvas.height - 10 || snake.slice(1).some((part) => part.x === snake[0].x && part.y === snake[0].y)) { clearInterval(gameInterval); ctx.fillStyle = '#fff'; ctx.font = '24px Pixel'; ctx.textAlign = 'center'; ctx.textBaseline = 'middle'; ctx.fillText('Game Over! Score: ' + score, canvas.width / 2, canvas.height / 2); return; } draw(); } canvas.width = 300; canvas.height = 300; gameInterval = setInterval(update, 100); draw(); 